# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven
name: Java CI with Maven

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    
    - uses: actions/cache@v1
      with:
        path: ~/.m2/repository
        key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
        restore-keys: |
          ${{ runner.os }}-maven-
    
    - name: Set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    
    - name: Print effective-settings (optional)
      run: mvn help:effective-settings
    
   
    - name: clean 
      run: mvn -U clean install  --settings ./conf/settings.xml
    - name: Build with Maven
      run: mvn -B package --file pom.xml
  deploy:
    needs: build
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - uses: actions/cache@v1
      with:
        path: ~/.m2/repository
        key: ${{ runner.os }}-mavven-${{ ('*.jar') }}
    - name: Deploy to CloudHub
        run: |
          mvn clean package deploy:deploy-file \
            -DmuleDeploy \
            -DmuleDeploy.prop.url=https://anypoint.mulesoft.com\
            -DmuleDeploy.prop.username=abhinav_0895 \
            -DmuleDeploy.prop.password=abhinavM@321 \
            -DmuleDeploy.prop.applicationName=cwb-intellect-banking \
            -DmuleDeploy.prop.environment=sandbox \
            -DmuleDeploy.prop.businessGroup=655004bb-38b3-4fc1-a751-02cc0cdcc6d8
